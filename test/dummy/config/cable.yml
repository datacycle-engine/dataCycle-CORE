development:
  adapter: async

test:
  adapter: async

staging:
  adapter: redis
  url: redis://<%= ENV['REDIS_SERVER'] %>:<%= ENV['REDIS_PORT'] %>/<%= ENV['REDIS_CABLE_DATABASE'] %>

production:
  adapter: redis
  url: redis://<%= ENV['REDIS_SERVER'] %>:<%= ENV['REDIS_PORT'] %>/<%= ENV['REDIS_CABLE_DATABASE'] %>
