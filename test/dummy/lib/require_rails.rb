# frozen_string_literal: true

# The dummy app's config/application.rb and every host project's already require this path, so it
# stays. The frameworks themselves are named in lib/data_cycle_core/require_rails, which the
# gemspec ships.
require 'data_cycle_core/require_rails'
