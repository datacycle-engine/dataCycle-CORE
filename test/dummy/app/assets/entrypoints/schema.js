// Named here for the same reason as openapi_viewer.js: the gem is at ../../ in the dummy,
// so dc_vite_entry_name's vendor/gems fallback cannot resolve the glob's key.
import "../../../../../app/assets/entrypoints/schema";
