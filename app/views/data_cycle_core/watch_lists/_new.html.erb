<div id="new-object-circle">
    <a data-toggle="new-object" class="button-prime"><i class="fa fa-plus" aria-hidden="true"></i></a>
    <div class="reveal" id="new-object" data-reveal data-append-to="#new-object-circle" data-v-offset="auto">
        <a class="option last" href="#" data-open="newWatchList"><i class="fa fa-folder-open-o" aria-hidden="true"></i>Neue Merkliste erstellen</a>
    </div>
</div>

<!-- testing -->

<% if !@watch_list.nil? %>

    <div class="reveal new-item" id="newWatchList" data-reveal>
        <h3>Neue Merkliste erstellen</h3>

        <%= form_for :watch_list, :url => {:action => "create"} do |f| %>

            <%= hidden_field_tag 'template', 'WatchList' %>
            <div class="validation-container form-element">
                <%= f.text_field :headline, class: 'form-control', value:'' %>
            </div>
            <%= f.submit "Erstellen", class: "button primary" %>

        <% end %>

        <button class="close-button" data-close aria-label="Close modal" type="button">
            <span aria-hidden="true">&times;</span>
        </button>
    </div>

<% end %>
