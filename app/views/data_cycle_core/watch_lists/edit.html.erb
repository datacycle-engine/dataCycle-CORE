<% breadcrumb :edit, @watch_list, :name %>

<% content_for :top_content do %>

<div>

    <div id="edit-form">

        <%= render 'form', keyPrefix: 'watch_list[datahash]', content: @watch_list %>

    </div>

</div>

<% end %>
