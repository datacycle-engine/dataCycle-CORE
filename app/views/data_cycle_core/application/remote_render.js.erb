$('<%= @target %>:visible').children().fadeOut('fast', function(){
  $('<%= @target %>:visible').html('<%= j render @partial, @options %>').trigger('remote-partial-rendered');
  $('<%= @target %>:visible').closest('.reveal[data-remote-path]:not(.loaded)').addClass('loaded');
});
