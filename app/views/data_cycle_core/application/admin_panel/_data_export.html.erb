<code class="formatted-json" data-json="<%= content.external_system_syncs.export.includes(:external_system).to_json(only: [:exported_data, :exception_data, :status, :last_sync_at, :last_successful_sync_at], include: { external_system: { only: [:id, :name, :identifier] } }) %>"></code>
