<% breadcrumb :edit_content, @content, @watch_list %>

<% content_for :top_content do %>

<div class="reveal-blur">

    <div id="edit-form">

        <% raise content.inspect %>
        <%= render 'form', keyPrefix: "#{@content.class.name.demodulize.underscore}[datahash]", content: @content %>

    </div>

</div>

<% end %>
