<%= tag.i class: 'fa fa-info-circle',
          data: {
            dc_tooltip: t("helper_text.attributes.#{content&.template_name}.#{key.attribute_name_from_key}.tooltip", locale: active_ui_locale)
          } if I18n.exists?("helper_text.attributes.#{content&.template_name}.#{key.attribute_name_from_key}.tooltip", locale: active_ui_locale) %>

<%= tag.i '*', class: 'required-field-marker', title: t('common.required_field', locale: active_ui_locale) if local_assigns[:definition]&.dig('validations', 'required') || local_assigns[:definition]&.dig('validations', 'min') %>
