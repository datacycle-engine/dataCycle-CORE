<%= thing_info_icon(content, key) %>

<%= tag.i '*', class: 'required-field-marker', data: { dc_tooltip: t('common.required_field', locale: active_ui_locale) } if local_assigns[:definition]&.dig('validations', 'required') || local_assigns[:definition]&.dig('validations', 'min') %>
