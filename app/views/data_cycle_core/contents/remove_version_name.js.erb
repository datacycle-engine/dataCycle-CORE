<% if @success_message.present? %>
  $('.named-version-container#version-name-<%= @content.id %>').remove();
  $('body').trigger('dc:flash:renderMessage', { type: 'success', text: '<%= t('feature.named_version.version_name_removed', locale: DataCycleCore.ui_language) %>' });
<% end %>
