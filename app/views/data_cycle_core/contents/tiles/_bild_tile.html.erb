<%= render 'data_cycle_core/contents/tiles/default_tile', local_assigns do %>
  <% if item.try(:thumbnail_url).present? %>
    <div class="image-thumbnail">
      <img data-src="<%= item.try(:thumbnail_url) %>" alt="<%= item.try(:title) %>" class="lazyload" />
    </div>
  <% end %>
<% end %>
