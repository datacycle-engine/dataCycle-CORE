<div class="grid-thumbnails <%= 'has-more' if item.image && item.image.try(:size) > 3 %>">
  <% item.image.first(3).each do |image| %>
      <img data-src="<%= image.thumbnail_url %>" alt="<%= image.headline %>" class="lazyload grid-item-thumbnail" />
  <% end %>
</div>
