
<% if !item.metadata.nil? && !item.metadata['validation'].nil? && !item.metadata['validation']['name'].nil? %>

    <div class="type <%= item.content_type.downcase %>">
        <div class="infoRow <%= "has-parent" if item.parent %>">
            <%= render 'data_cycle_core/shared/icon', icon_type: item.content_type.downcase %>
            <% if item.content_type.downcase == 'contentlocation' %>
                <span class="title">Ort</span>
            <% else %>
                <span class="title"><%= item.content_type %></span>
            <% end %>
            <span class="locale">(<%= I18n.locale.to_s %>)</span>
        </div>
    </div>

<% else %>

    <div class="type">
        <div class="infoRow">
            <span class="icon">
                <i class="fa fa-folder-open-o" aria-hidden="true"></i>
            </span>
            <span class="title">Undefined Content</span>
        </div>
    </div>

<% end %>
