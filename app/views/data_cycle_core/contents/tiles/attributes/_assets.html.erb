<% if item.asset.first.thumbnail_url? %>
    <div class="image-thumbnail">
        <img data-src="<%= item.asset.first.thumbnail_url %>" alt="<%= item.headline %>" class="lazyload grid-item-thumbnail" />
    </div>
<% end %>