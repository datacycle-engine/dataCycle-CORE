<% I18n.with_locale(locale) do %>
    <% internal_template = DataCycleCore::DataHashService.get_internal_template(definition['linked_table'], definition['template_name']) %>
    <% validation_properties = ordered_validation_properties(validation: internal_template.schema) %>

    <div class="content-object-item textblock" id="<%= sanitize_to_id(key) %>_item_<%= index %>">
    <span class="embedded-header form-element validation-container">
      <span class="draggable-handle"><i class="fa fa-bars" aria-hidden="true"></i></span>
        <%= label_tag "#{key}[#{index}][name]", validation_properties.dig('name', 'label') %>
        <%= text_field_tag "#{key}[#{index}][name]", nil, options.merge({ class: 'form-control', placeholder: validation_properties.dig('name', 'label') }) %>
    </span>

        <% if can?(:edit, DataCycleCore::DataAttribute.new(key, definition, options, internal_template, :edit)) %>
            <button type="button" class="button removeContentObject"><i class="fa fa-times"></i></button>
        <% end %>

        <%= render_attribute_editor key: "#{key}[#{index}][text]", definition: validation_properties['text'], value: nil, parameters: { options: (validation_properties['text'].presence&.dig('editor', 'options') || {})}, content: internal_template %>
    </div>
<% end %>
