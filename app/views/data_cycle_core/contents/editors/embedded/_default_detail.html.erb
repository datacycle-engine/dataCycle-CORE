<% if object.schema.present? %>

    <% validation_properties = ordered_validation_properties(validation: object.schema) %>
    <% if !validation_properties.nil? %>

        <div class="content-object-item draggable_<%= sanitize_to_id(key) %>" id="<%= sanitize_to_id(key) %>_item_<%= index %>" data-id="<%= object.id %>" data-index="<%= index %>">

            <span class="embedded-header">
              <% if can?(:edit, DataCycleCore::DataAttribute.new(key, definition, options, content, :edit)) %>
                <span class="draggable-handle"><i class="fa fa-bars" aria-hidden="true"></i></span>
                <span class="swap-prev swap-button disabled" title="<%= t('common.embedded_move_up', locale: DataCycleCore.ui_language) %>" data-tooltip><i class="fa fa-arrow-up" aria-hidden="true"></i></span>
                <span class="swap-next swap-button disabled" title="<%= t('common.embedded_move_down', locale: DataCycleCore.ui_language) %>" data-tooltip><i class="fa fa-arrow-down" aria-hidden="true"></i></span>
              <% end %>
              <%= render 'data_cycle_core/shared/icon', icon_type: object.template_name.downcase %> <%= definition['label'] %>
              <%= render 'data_cycle_core/contents/helper_text', local_assigns %>
            </span>

            <% if can?(:edit, DataCycleCore::DataAttribute.new(key, definition, options, content, :edit)) %>
              <%= button_tag tag.i(class: 'fa fa-times'), type: :button, class: 'button removeContentObject', data: {
                confirm_delete: definition&.dig('translated') && object&.translations&.many? ? t('actions.delete_with_translated_html', locale: DataCycleCore.ui_language) : nil
              } %>
            <% end %>

            <%= hidden_field_tag("#{key}[#{index}][id]", object.id) unless local_assigns[:new_content] %>

            <% validation_properties.each do |o_key, o_prop| %>
                <% value = I18n.with_locale(local_assigns[:attribute_locale]){ object.try(o_key.to_sym) } %>
                <%= render_attribute_editor key: "#{key}[#{index}][#{o_key}]", definition: o_prop, value: value, parameters: { options: (o_prop.dig('ui', 'edit', 'options') || {}).merge(prefix: local_assigns.dig(:options, :prefix)), new_content: local_assigns[:new_content], parent: object, attribute_locale: local_assigns[:attribute_locale] }, content: content %>
            <% end %>
        </div>

    <% end %>
<% end %>
