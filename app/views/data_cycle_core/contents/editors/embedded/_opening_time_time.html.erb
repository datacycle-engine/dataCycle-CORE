<%= tag.div class: "content-object-item draggable_#{sanitize_to_id(key)} accordion-item is-active",
  id: "#{sanitize_to_id(key)}_item_#{index}",
  data: {
    id: local_assigns[:object]&.id,
    index: index,
    accordion_item: true
  } do %>

  <%
    readonly = !attribute_editable?(key, definition, local_assigns[:options], content)
    new_key = "#{key}[#{index}]"
  %>

  <span class="embedded-header">
    <% unless readonly %>
      <span class="draggable-handle"><i class="fa fa-bars" aria-hidden="true"></i></span>
      <span class="swap-prev swap-button disabled"
        title="<%= t('embedded.move_up', locale: DataCycleCore.ui_language) %>"><i
          class="fa fa-arrow-up" aria-hidden="true"></i></span>
      <span class="swap-next swap-button disabled"
        title="<%= t('embedded.move_down', locale: DataCycleCore.ui_language) %>"><i
          class="fa fa-arrow-down" aria-hidden="true"></i></span>
    <% end %>
  </span>

  <a href="#" class="accordion-title">
    <span class="icon"><i class="fa fa-calendar" aria-hidden="true"></i></span>
    <span class="embedded-label"><%= definition['label'] %></span>
    <%= render 'data_cycle_core/contents/helper_text', local_assigns %>
  </a>

  <% unless readonly %>
    <%= button_tag tag.i(class: 'fa fa-times'), type: :button, class: 'button removeContentObject', data: {
          confirm_delete: definition&.dig('translated') && local_assigns[:object]&.translations&.many? ? t('actions.delete_with_translated_html', locale: DataCycleCore.ui_language) : nil
        } %>
  <% end %>

  <%= hidden_field_tag("#{new_key}[id]", local_assigns[:object].id) unless local_assigns[:duplicated_content] || local_assigns[:object].nil? %>

  <% value = local_assigns[:object]&.to_h || {} %>

  <div class="opening-time-time embedded-properties accordion-content" data-tab-content>
    <div class="daterange opening-time-time-range">
      <div class="form-element opens">
        <%= label_tag "#{local_assigns.dig(:options, :prefix)}#{sanitize_to_id("#{new_key}[opens]")}" do %>
          <%= content_tag(:i, nil, class: 'fa fa-ban', aria_hidden: true) if readonly %>
          <%= t('opening_time.opens', locale: DataCycleCore.ui_language) %>
        <% end %>
        <% if readonly %>
          <%= text_field_tag "#{new_key}[opens]",
            l(value.dig(:start_time, :time)&.in_time_zone, format: :time_only, locale: DataCycleCore.ui_language),
            class: 'read-only date',
            id: "#{local_assigns.dig(:options, :prefix)}#{sanitize_to_id("#{new_key}[opens]")}", readonly: true %>
        <% else %>
          <%= time_field_tag "#{new_key}[opens]", l(value.dig(:start_time, :time)&.in_time_zone, format: :time_only, locale: DataCycleCore.ui_language), id: "#{local_assigns.dig(:options, :prefix)}#{sanitize_to_id("#{new_key}[opens]")}", data: { type: 'timepicker' } %>
        <% end %>
      </div>

      <div class="form-element closes">
        <%= label_tag "#{local_assigns.dig(:options, :prefix)}#{sanitize_to_id("#{new_key}[closes]")}" do %>
          <%= content_tag(:i, nil, class: 'fa fa-ban', aria_hidden: true) if readonly %>
          <%= t('opening_time.closes', locale: DataCycleCore.ui_language) %>
        <% end %>

        <% if readonly %>
          <%= text_field_tag "#{new_key}[closes]",
            l(value.dig(:end_time, :time)&.in_time_zone, format: :time_only, locale: DataCycleCore.ui_language),
            class: 'read-only date',
            id: "#{local_assigns.dig(:options, :prefix)}#{sanitize_to_id("#{new_key}[closes]")}", readonly: true %>
        <% else %>
          <%= time_field_tag "#{new_key}[closes]", l(value.dig(:end_time, :time)&.in_time_zone, format: :time_only, locale: DataCycleCore.ui_language), id: "#{local_assigns.dig(:options, :prefix)}#{sanitize_to_id("#{new_key}[closes]")}", data: { type: 'timepicker' } %>
        <% end %>
      </div>
    </div>
  </div>
<% end %>
