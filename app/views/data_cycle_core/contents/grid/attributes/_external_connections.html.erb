<%= tag.i(
  class: "dc-foundation-tooltip fa fa-external-link-square has-tip external-connections-icon#{' success' unless content.try(:external_source).nil?} #{content.try(:external_system_syncs)&.pluck(:status)&.compact&.join(' ')}",
  title: external_systems_tooltip(content.try(:external_source), content.try(:external_system_syncs))
  ) if !content.try(:external_source).nil? || content.try(:external_system_syncs)&.any? %>
