<%= render_linked_details(
      key: local_assigns[:key],
      definition: local_assigns[:definition],
      parameters: {
        object: @object,
        options: local_assigns[:options]
      }
    ) %>
