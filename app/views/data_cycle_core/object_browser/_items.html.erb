<% contents.each do |content| %>
  <%= render_object_browser_partial(key: key + (definition['type'] == "object" ? '[][id]' : '[]'), definition: definition, parameters: {object: content, options: options, objects: objects}) %>
<% end %>
