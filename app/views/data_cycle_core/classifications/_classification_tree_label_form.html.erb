<%= form_for(classification_tree_label, url: classifications_path, remote: true) do |f| %>
  <%= f.hidden_field :id %>
  <%= f.text_field :name %>
  <%= f.check_box :internal %>
  <%= f.submit %>
<% end %>
