# frozen_string_literal: true

json ||= {}
key_new = definition.dig('api', 'name') || key.camelize(:lower)

if content.event_schedule.present?
  data = content.event_schedule.to_a
  json[key_new] = data.map(&:to_schedule_schema_org)
end

json
