# frozen_string_literal: true

json = {}

json['data'] = @concept_schemes
  .as_json(
    only: [:id, :name]
  ).map { |item| item.merge('url' => api_v3_classification_tree_url(@api_subversion, id: item['id'], language: @language)) }

json.merge! render 'pagination_links',
                   objects: @concept_schemes,
                   object_url: ->(params) { api_v3_classification_trees_url(params) }

json
