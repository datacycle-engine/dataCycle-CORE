# frozen_string_literal: true

json = {
  data: @concept_scheme.as_json(
    only: [:id, :name]
  ).merge('classifications' => classifications_api_v3_classification_tree_url(@api_subversion, id: @concept_scheme, language: @language))
}

json
