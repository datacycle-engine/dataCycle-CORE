# frozen_string_literal: true

json.set! 'message', 'Jbuilder bug, check Api::V2::WatchListsController'

# json.watch_lists @watch_lists do |item|
#   json.set! 'id', item.id
#   json.set! 'user_id', item.user_id
#   json.partial! 'items', locals: {item: item }
# end

# json.set! "total", @total
