# frozen_string_literal: true

json ||= {}
key_new = attribute_key(key, definition)

concepts = content.send(:"#{key}_overlay")
classification_string = concepts
  .presence.map { |concept| concept.uri || concept.name || concept.internal_name }
  .join(',')

json = { key_new => classification_string } if classification_string.present?
json
