# frozen_string_literal: true

json ||= {}
key_new = attribute_key(key, definition)

concepts = content.send(:"#{key}_overlay")
res = concepts.presence.map { |concept| concept.uri.presence || concept.name.presence || concept.internal_name }

json = { key_new => res } if res.present?
json
