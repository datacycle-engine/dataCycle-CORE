<div>
  <div class="card">
    <div class="card-divider">
      <div class="header">
        <h5 class="copy-to-clipboard" data-value="<%= external_system[:name] %>" title="<%= external_system[:name] %>"><%= external_system[:name] %></h5>
        <h5 class="copy-to-clipboard identifier" data-value="<%= external_system[:identifier] %>" title="<%= external_system[:identifier] %>"><%= external_system[:identifier] %></h5>
      </div>
      <h6 class="external-system-id">
        <span class="copy-to-clipboard" data-value="<%= external_system[:id] %>"><%= external_system[:id] %></span>
      </h6>
    </div>
  </div>
</div>
