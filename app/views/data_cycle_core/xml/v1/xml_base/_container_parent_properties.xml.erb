<% if content&.parent && content&.parent&.content_type?('container') && options.dig(:disable_parent).blank? %>
  <schema:isPartOf>
    <%= render content_partial()'details', content: content.parent, options: options.merge({ disable_children: true })) %>
  </schema:isPartOf>
<% end %>
