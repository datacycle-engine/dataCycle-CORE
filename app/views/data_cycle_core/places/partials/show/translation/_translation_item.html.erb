<% unless translation.nil? %>

    <% unless translation.name.nil? %>
        <%= render 'data_cycle_core/creative_works/partials/show/datatype/string',
                   label: dataCycleObject.metadata['validation']['properties']['name']['label'],
                   prop: dataCycleObject.metadata['validation']['properties']['name'],
                   key: 'name',
                   value: translation.name %>
    <% end %>

<% end %>
