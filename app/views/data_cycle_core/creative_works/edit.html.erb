
<% content_for :top_content do %>

    <div class="detail-header">

      <div class="type thema">
          <span class="icon">
            <i class="fa fa-folder-open-o" aria-hidden="true"></i>
          </span>
        <span class="title">Thema</span>
      </div>

      <a class="bookmark" href="#">
        <span class="bookmarkinfo">zur Recherche hinzufügen</span>
        <i class="fa fa-bookmark-o" aria-hidden="true"></i>
      </a>

      <div class="title">
        <%= @creativeWork.headline %>
      </div>
      <div class="description">
        <% if !@creativeWork[:description].nil? %>
            <%= @creativeWork.description.html_safe %>
        <% end %>
      </div>

      <div class="tags">
        <% @creativeWork.classification_aliases.each do |classificationAlias| %>
            <span class="tag"><a href="#"><%= classificationAlias.name %></a></span>
        <% end %>
      </div>

      <div class="edit">
        <%= link_to 'Bearbeiten >', edit_creative_work_path(@creativeWork), class:'button primary' %>
      </div>

    </div>

<% end %>

<%= form_for(@creativeWork) do |f| %>

    <%= f.text_field :headline, class: 'form-control' %>


    <div class="editor-block">
      <%= f.hidden_field :description %>
      <%= f.fields_for :description do |form|%>

          <div class="quill-editor" data-hidden-field-id="<%= "#{form.object_name}".gsub(/(\])?\[/, "_").chop %>">
            <% if !@creativeWork[:description].nil? %>
              <%= @creativeWork[:description].html_safe %>
            <% end %>

          </div>

      <% end %>

    </div>

    <%= f.submit "Update", class: "button primary" %>
<% end %>
